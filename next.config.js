/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com'], // Add the domain you want to whitelist
    },
  };
  
  export default nextConfig;
  